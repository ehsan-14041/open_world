# Legacy modules

Code under `legacy/` is **not wired to the product UI** (`/` operations decision simulator).

The `legacy/startup/` tree preserves the former founder/startup SKU for reference only.
